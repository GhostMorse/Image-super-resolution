torch>=2.0
torchvision>=0.15
numpy>=1.23
scipy>=1.10            # reference interpolation in the tests
albumentations>=1.3   # training augmentations
opencv-python-headless>=4.7
Pillow>=9.0
torchmetrics>=1.0     # F1 during training
tqdm>=4.64

# Development / tests
pytest>=7.0

# Optional (SR-quality evaluation part of the assignment, not required by this package)
# lpips
# plotly
# ipywidgets
